# входной поток для uWSGI
upstream uwsgi_app {
    server unix:/code/perfumancer/uwsgi_app.sock;
}

server {
    listen              80;
    listen              443 ssl;
    ssl_certificate     /code/perfumancer/ssl/perfumancer.crt;
    ssl_certificate_key /code/perfumancer/ssl/perfumancer.key;
    server_name         www.perfumancer.ru perfumancer.ru;
    error_log           stderr warn;
    access_log          /dev/stdout main;

    location / {
        include     /etc/nginx/uwsgi_params;
        uwsgi_pass  uwsgi_app;
    }

    location /static/ {
        alias /code/perfumancer/static/;
    }

    location /media/ {
        alias /code/perfumancer/media/;
    }
}